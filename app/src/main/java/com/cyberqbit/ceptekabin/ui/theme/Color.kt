package com.cyberqbit.ceptekabin.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════
//  MONOKROM BASE PALETTE — Beyaz, Siyah, Gri Tonları
// ═══════════════════════════════════════════════════════
val White       = Color(0xFFFFFFFF)
val Black       = Color(0xFF000000)
val Grey50      = Color(0xFFFAFAFA)
val Grey100     = Color(0xFFF5F5F5)
val Grey200     = Color(0xFFEEEEEE)
val Grey300     = Color(0xFFE0E0E0)
val Grey400     = Color(0xFFBDBDBD)
val Grey500     = Color(0xFF9E9E9E)
val Grey600     = Color(0xFF757575)
val Grey700     = Color(0xFF616161)
val Grey800     = Color(0xFF424242)
val Grey900     = Color(0xFF212121)

// ═══════════════════════════════════════════════════════
//  FASHION PALETTE — Modern Moda Temalı Renkler
// ═══════════════════════════════════════════════════════
val PrimaryTeal        = Color(0xFF006D77)   // Deep Teal — ana renk
val SecondaryTerracotta= Color(0xFFE8A598)   // Warm Terracotta — ikincil renk

// ═══════════════════════════════════════════════════════
//  BACKGROUNDS
// ═══════════════════════════════════════════════════════
val BackgroundDark     = Color(0xFF0D1117)   // Koyu arka plan
val BackgroundLight    = Color(0xFFF8F5F2)   // Açık arka plan

// ═══════════════════════════════════════════════════════
//  SURFACES
// ═══════════════════════════════════════════════════════
val SurfaceDark        = Color(0xFF161B22)   // Koyu yüzey
val SurfaceLight       = Color(0xFFFFFFFF)   // Açık yüzey
val SurfaceVariantDark = Color(0xFF1E2530)   // Koyu varyant
val SurfaceVariantLight= Color(0xFFF0EDE8)   // Açık varyant

// ═══════════════════════════════════════════════════════
//  LIQUID GLASS — Dark Mode (Glassmorphism)
// ═══════════════════════════════════════════════════════
val GlassDark          = Color(0x20FFFFFF)   // %12 beyaz — ana cam yüzeyi
val GlassDarkSurface   = Color(0x18FFFFFF)   // %9 beyaz — alt yüzey
val GlassDarkBorder    = Color(0x33FFFFFF)   // %20 beyaz — kenar çizgisi
val GlassDarkHighlight = Color(0x40FFFFFF)   // %25 beyaz — parlak nokta
val GlassDarkShadow    = Color(0x55000000)   // koyu gölge

// ═══════════════════════════════════════════════════════
//  LIQUID GLASS — Light Mode (Glassmorphism)
// ═══════════════════════════════════════════════════════
val GlassLight         = Color(0x15000000)   // %8 siyah — ana cam yüzeyi
val GlassLightSurface  = Color(0x08000000)   // neredeyse görünmez
val GlassLightBorder   = Color(0x20000000)   // %12 siyah — kenar çizgisi
val GlassLightHighlight= Color(0x28000000)   // %16 siyah — parlak nokta

// ═══════════════════════════════════════════════════════
//  ACCENT — Altın / Teal Vurgular
// ═══════════════════════════════════════════════════════
val AccentGold         = Color(0xFFD4A853)   // Gold — lüks his
val AccentGoldLight    = Color(0xFFE8C878)   // Açık altın
val AccentSilver       = Color(0xFFA8A9AD)   // Soft gümüş
val Accent             = Color(0xFFE8A598)   // Terracotta accent

// ═══════════════════════════════════════════════════════
//  TEXT COLORS
// ═══════════════════════════════════════════════════════
val TextPrimaryDark    = Color(0xFFEAEAEA)   // Koyu mod — birincil metin
val TextSecondaryDark  = Color(0xFF8B949E)   // Koyu mod — ikincil metin
val TextPrimaryLight   = Color(0xFF1A1A2E)   // Açık mod — birincil metin
val TextSecondaryLight = Color(0xFF57606A)   // Açık mod — ikincil metin

// ═══════════════════════════════════════════════════════
//  SEMANTIC — Durum Renkleri
// ═══════════════════════════════════════════════════════
val Success            = Color(0xFF4CAF50)   // Yeşil
val SuccessLight       = Color(0xFF81C784)
val Error              = Color(0xFFE53935)   // Kırmızı
val ErrorLight         = Color(0xFFEF5350)
val Warning            = Color(0xFFFF9800)   // Turuncu
val WarningLight       = Color(0xFFFFB74D)
val Info               = Color(0xFF29B6F6)   // Mavi

// ═══════════════════════════════════════════════════════
//  PRIMARY — Deep Teal (Moda / Fashion)
// ═══════════════════════════════════════════════════════
val PrimaryDark        = Color(0xFF006D77)   // Deep Teal — primary (dark mode)
val PrimaryLight       = Color(0xFF008B96)   // Açık Teal — primary (light mode)
val OnPrimaryDark      = Color(0xFFFFFFFF)
val OnPrimaryLight     = Color(0xFFFFFFFF)

// ═══════════════════════════════════════════════════════
//  SECONDARY — Warm Terracotta
// ═══════════════════════════════════════════════════════
val SecondaryDark      = Color(0xFFE8A598)   // Terracotta
val SecondaryLight     = Color(0xFFD4846F)   // Koyu Terracotta
val OnSecondaryDark    = Color(0xFF1A1A1A)
val OnSecondaryLight   = Color(0xFFFFFFFF)

// ═══════════════════════════════════════════════════════
//  WEATHER — Hava durumu ikon renkleri
// ═══════════════════════════════════════════════════════
val WeatherSunny       = Color(0xFFFFC107)
val WeatherRainy       = Color(0xFF607D8B)
val WeatherCloudy      = Color(0xFF90A4AE)
val WeatherSnowy       = Color(0xFFB3E5FC)
val WeatherStormy      = Color(0xFF37474F)

// ═══════════════════════════════════════════════════════
//  KIYAKET TURU — Kategori renkleri
// ═══════════════════════════════════════════════════════
val TurUstGiyim        = Color(0xFF006D77)   // Teal
val TurAltGiyim        = Color(0xFF26A69A)   // Light Teal
val TurDisGiyim        = Color(0xFF8D6E63)   // Brown
val TurAyakkabi        = Color(0xFF78909C)   // Blue Grey
val TurAksesuar        = Color(0xFFE8A598)   // Terracotta
